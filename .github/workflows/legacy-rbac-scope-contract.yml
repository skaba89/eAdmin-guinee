name: Legacy RBAC Scope Contract

on:
  pull_request:
    paths:
      - 'src/lib/rbac.ts'
      - 'src/lib/service-request-scope.ts'
      - 'src/lib/frontend-role-authority.ts'
      - 'scripts/check-legacy-rbac-scope-contract.ts'
      - '.github/workflows/legacy-rbac-scope-contract.yml'
  push:
    branches: [main]
    paths:
      - 'src/lib/rbac.ts'
      - 'src/lib/service-request-scope.ts'
      - 'src/lib/frontend-role-authority.ts'
      - 'scripts/check-legacy-rbac-scope-contract.ts'
      - '.github/workflows/legacy-rbac-scope-contract.yml'

permissions:
  contents: read

jobs:
  legacy-scope-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.4

      - name: Verify canonical levels and signed legacy scopes
        run: bun scripts/check-legacy-rbac-scope-contract.ts
