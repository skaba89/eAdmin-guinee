# =============================================================================
# eAdministration Suite Guinea — Dépendances de développement / test
# =============================================================================
# Installer avec : pip install -r requirements-dev.txt

-r requirements.txt

# --- Tests ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
aiosqlite>=0.20.0

# --- Qualité du code ---
# Pin exact pour que le quality gate ne change pas au gré d'une nouvelle release.
ruff==0.15.22
mypy>=1.10.0
