# =============================================================================
# eAdministration Suite Guinea — Dépendances de développement / test
# =============================================================================
# Installer avec : pip install -r requirements-dev.txt

# Inclure les dépendances de production
-r requirements.txt

# --- Tests ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-httpx>=0.30.0
aiosqlite>=0.20.0
httpx>=0.27.0

# --- Qualité du code ---
ruff>=0.4.0
mypy>=1.10.0
