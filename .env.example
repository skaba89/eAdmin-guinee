# =============================================================================
# eAdministration Suite Guinea — Root Environment Variables
# ⚠️ NEVER commit real secrets to Git!
# Copy this file to .env and fill in your values.
# =============================================================================

# --- PostgreSQL ---
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD
POSTGRES_DB=eadmin
POSTGRES_USER=eadmin

# --- Redis ---
REDIS_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD

# --- JWT / App Secret ---
# Generate: python -c 'import secrets; print(secrets.token_urlsafe(48))'
SECRET_KEY=CHANGE_ME_GENERATE_WITH_command_above

# --- MinIO Object Storage ---
MINIO_ROOT_USER=eadmin
MINIO_ROOT_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD

# --- Grafana ---
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD
GRAFANA_ROOT_URL=https://monitoring.eadmin.gouv.gn

# --- Environment ---
ENVIRONMENT=development

# --- Frontend ---
# Local development:
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=eAdministration Suite Guinea
# Render/production example (use the real backend service hostname, no /api/v1 suffix):
# NEXT_PUBLIC_API_URL=https://<backend-service>.onrender.com

# --- Backend CORS ---
# JSON array of additional browser origins allowed to call the API.
# In staging/production only explicit HTTPS origins are accepted; wildcard * is refused.
EXTRA_CORS_ORIGINS=[]
# Render example (use the real frontend service hostname):
# EXTRA_CORS_ORIGINS=["https://<frontend-service>.onrender.com"]

# --- First production SUPER_ADMIN bootstrap (optional, one-time) ---
# No default password exists in source. Enable only for the first deployment when
# the database has no SUPER_ADMIN, then remove/disable these variables after the
# first successful administrator login.
EADMIN_BOOTSTRAP_SUPERADMIN_ENABLED=false
# EADMIN_BOOTSTRAP_SUPERADMIN_EMAIL=admin@example.gov.gn
# EADMIN_BOOTSTRAP_SUPERADMIN_PASSWORD=CHANGE_ME_STRONG_12_PLUS
# EADMIN_BOOTSTRAP_SUPERADMIN_NAME=Super Administrateur eAdmin

# --- Non-production portal test accounts (for staging/test only) ---
# Creates idempotent test accounts for CITOYEN, AGENT, MAIRIE, AGENCE, ADMIN,
# CHEF_SERVICE, DIRECTEUR and MINISTRE. The application refuses this flag when
# ENVIRONMENT=production.
EADMIN_BOOTSTRAP_TEST_USERS=false
# EADMIN_BOOTSTRAP_TEST_PASSWORD=CHANGE_ME_STRONG_12_PLUS
EADMIN_BOOTSTRAP_TEST_DOMAIN=test.eadmin.local
EADMIN_BOOTSTRAP_TEST_INSTITUTION=Institution Pilote eAdmin
EADMIN_BOOTSTRAP_TEST_INSTITUTION_ID=institution-test

# --- Frontend Encryption Key (for client-side AES-256-GCM) ---
# Generate: python -c 'import secrets; print(secrets.token_urlsafe(32))'
NEXT_PUBLIC_ENCRYPTION_KEY=CHANGE_ME_GENERATE_WITH_command_above

# =============================================================================
# DOCKER COMPOSE USAGE
# =============================================================================
# Development: docker compose -f docker-compose.yml -f docker-compose.dev.yml up
# Production:  docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# All secrets are passed via environment variables — no hardcoded values.
# Production compose uses ${VAR:?required} to fail fast if a secret is missing.
# =============================================================================
